STR = {('|").*('|")}
WORD = {([a-zA-Z_]+)([a-zA-Z_0-9]*)}
NUMBER = {(\-?)([0-9\.]+)}
OP = {[\+\-\*\/%]}
VAL = (STR|NUMBER|WORD)

EXPR = VAL (OP VAL)+
VAR_DEF = WORD {\s*} '=' {\s*} VAL
ANY = VAR_DEF|EXPR|VAL
FUNCTION = WORD '(' (((ANY{,?}{\s*}) {[^,]})*)|ANY ')'
ROUTE = '@' WORD '(' STR|WORD '):'
